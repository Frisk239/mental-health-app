# ===========================================
# 心灵健康助手 - 环境变量配置
# ===========================================
# 复制此文件为 .env 并填写实际值
# cp .env.example .env

# ===========================================
# 数据库配置
# ===========================================
# SQLite数据库 (推荐用于开发)
DATABASE_URL=sqlite:///./mental_health.db

# PostgreSQL数据库 (生产环境推荐)
# DATABASE_URL=postgresql://username:password@localhost:5432/mental_health_app

# ===========================================
# Redis配置 (可选，用于缓存)
# ===========================================
# Redis服务器地址
REDIS_URL=redis://localhost:6379

# Redis密码 (如果设置了密码)
# REDIS_PASSWORD=your-redis-password

# ===========================================
# JWT配置 (用于用户认证)
# ===========================================
# JWT密钥 (生产环境请使用强密码)
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-please

# JWT算法
ALGORITHM=HS256

# Token过期时间 (分钟)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# AI模型配置
# ===========================================
# DeepSeek API密钥 (必须配置)
# 获取地址: https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# HuggingFace API密钥 (可选，用于其他AI模型)
HUGGINGFACE_API_KEY=your-huggingface-api-key-here

# ===========================================
# 应用配置
# ===========================================
# 调试模式 (开发环境设为true，生产环境设为false)
DEBUG=True

# CORS允许的源 (前端开发服务器地址)
CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]

# 生产环境前端地址
# CORS_ORIGINS=["https://yourdomain.com"]

# ===========================================
# 文件上传配置
# ===========================================
# 最大上传文件大小 (字节)
MAX_UPLOAD_SIZE=10485760

# 上传目录
UPLOAD_DIR=uploads/

# ===========================================
# 隐私和安全配置
# ===========================================
# 启用联邦学习 (实验性功能)
ENABLE_FEDERATED_LEARNING=False

# 数据保留天数
DATA_RETENTION_DAYS=365

# ===========================================
# 第三方服务配置 (可选)
# ===========================================
# 邮件服务配置 (用于通知功能)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# 监控服务配置 (可选)
# SENTRY_DSN=your-sentry-dsn-here

# ===========================================
# 开发环境专用配置
# ===========================================
# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 启用SQL查询日志 (开发环境有用)
SQL_ECHO=False

# ===========================================
# 配置说明
# ===========================================
#
# 1. DeepSeek API密钥获取:
#    - 访问: https://platform.deepseek.com/
#    - 注册账号并完成实名认证
#    - 在控制台创建API密钥
#    - 复制密钥填入 DEEPSEEK_API_KEY
#
# 2. 数据库选择:
#    - 开发环境: 使用SQLite (DATABASE_URL=sqlite:///./mental_health.db)
#    - 生产环境: 使用PostgreSQL (需要安装PostgreSQL服务器)
#
# 3. Redis配置:
#    - 可选功能，用于缓存和会话存储
#    - 开发环境可以不配置
#    - 生产环境推荐配置
#
# 4. 安全注意事项:
#    - 生产环境请使用强密码
#    - 不要将 .env 文件提交到Git仓库
#    - 定期轮换API密钥
#
# 5. 环境变量加载:
#    - 确保安装了 python-dotenv
#    - 应用启动时会自动加载 .env 文件
#
# ===========================================
